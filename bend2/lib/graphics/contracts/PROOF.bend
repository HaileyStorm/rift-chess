# Closed proofs for the narrow reusable graphics contracts.
import Base
import ../Canvas.bend as Canvas
import ../Font.bend as Font
import ./LAWS.bend as Laws

def Laws.draw_empty(depth, x, y, scale, color, image):
  {==}

def Laws.width_empty(scale):
  {==}

def Laws.rect_zero_width(depth, x, y, height, color, image):
  {==}

def Laws.source_subtree_uses_source_depth(a, b, c, d, e, f, g):
  {==}

def Laws.embed_identity(source, target):
  {==}
