# DRAFT relocated proofs; checker receipt is not frozen law or device evidence.
import Base
import ./LAWS.bend as Laws

def Laws.masked_none(depth, size, light, dark, image):
  {==}

def Laws.tile_absent(light, dark, absent):
  {==}

def Laws.tile_even(light, dark, absent):
  {==}

def Laws.tile_odd(light, dark, absent):
  {==}

def Laws.spatial_empty(depth, size, projection, palette, image):
  {==}

def Laws.spatial_lit_empty(depth, size, projection, palette, light, image):
  {==}

def Laws.spatial_illuminate_zero(style, ink, file, rank,
  origin_file, origin_rank):
  {==}

def Laws.spatial_fast_empty(depth, size, affine, palette, image):
  {==}
