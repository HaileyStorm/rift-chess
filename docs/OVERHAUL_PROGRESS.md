# Overhaul progress — 2026-09-07

**Overall: approximately 67%. Goal active. No replacement release is approved or published.** The full contract is [OVERHAUL_PLAN.md](OVERHAUL_PLAN.md), including the owner's correction that substantial WOW factor is mandatory beyond geometry/UI repair.

## Evidence from this iteration

- Reproduced the reported Shift failure class on the public v1 release and a frozen matching local build: clicking the legal lone passenger in ordinary mode produces the movable-piece warning; explicitly switching mode selects the platform. This is a reproduced interaction defect, not reconstruction of the owner's exact saved game. Local evidence: `.artifacts/overhaul/baseline/`.
- Rebuilt piece assets as closed, inspectable components with volumetric knight forms and differentiated Classic/Faceted profiles. Components merge into one draw call per piece while retaining audit metadata. Three new topology/material tests pass. Visual sculpture quality is still under review.
- Replaced the busy dual-rail UI with a board-focused layout, action dock, contextual passenger action, explicit Shift preview/confirmation, and secondary drawers. Preserved local modes, save/replay, both hotseat actors, tutorials and keyboard controls. Added legal-action caching and animation-epoch guards. The public release is unchanged.
- Added distinct world builders, a deep mechanical understructure, an opening/exploration camera, shared geometry/material reuse, controlled bloom and new Shift/capture/promotion choreography. Reduced motion and skip paths remain. These are implemented candidates, not proof of premium quality.
- Type-check/production build and all **38 tests in five files** passed during integration. Existing rules/reference fixtures are unchanged. These tests do not constitute visual or complete-product acceptance.
- `scripts/overhaul-smoke.mjs` passed the actual rendered blocked-passenger route: visible contextual action, source selection, hole preview, explicit confirmation, knight promotion before atomic commitment, and direct passenger selection in explicit Shift mode. Inactive replay controls are hidden. Screenshots/video/receipt: `.artifacts/overhaul/integrated-slice/`. This is a narrow intermediate check, not the 18-session matrix.

## Visual review: first slice failed

An independent reviewer inspected the actual initial entrance/Gallery/Nocturne/Daylight captures and marked the slice **FAIL** against the WOW requirements. The main thread agreed. Findings:

1. Worlds read as the same arena with scattered primitive props and color changes, without coherent architectural silhouettes.
2. The rift read as a black rectangle with rings/disc decoration rather than convincing depth and machinery.
3. The large entrance headline obscured the board; cropped background forms distracted from it.
4. Large poles/loops lacked visible connections and a clear architectural purpose.
5. Tile/material craft and lighting remained too generic, with weak ivory/dark detail separation.

Subsequent changes rebuilt Gallery as a connected vaulted chamber with an aisle, terraces, oculus and grounded structural components. Nocturne now has an observatory hemisphere, supported astrolabe, animated celestial globe and star sea; Daylight has a terraced reflecting court, causeway, reservoir and solid barrel-vault pavilions. Original bundled marble texture, world-derived reflections and a camera-dependent rift field replace the flat surface treatment. Piece sculpture received another refinement pass. Entrance typography and camera composition were rebuilt, and a detected play-layout regression that hid the header/action dock was repaired.

The independent second review of `.artifacts/overhaul/world-review-2/` supports proceeding into hierarchical inspection, but **FAILS final WOW acceptance**. That review identified four major findings: Daylight fog/exposure washes away architecture, Nocturne's planet dominates the board, Gallery architecture is too dark relative to its oculus, and the rift still reads as a shallow tray. Their correction is documented below; the original failed evidence is retained. The 14-capture run has zero console/page errors and verifies visible desktop play controls, blocked Explore commits, return to play and existing-save bypass of the entrance. It does not establish full visual acceptance.

The original texture's provenance is recorded in [ASSET_LEDGER.md](ASSET_LEDGER.md). No runtime network asset or paid inference was introduced. Production build and the 38-test suite passed after this integration; the complete later acceptance matrices remain pending.

## Hierarchical and motion corrections in progress

- The third world capture exposed a large solid shaft exterior above the floor and disconnected Gallery members; the new wall depth also revealed the floor's front edge. Daylight's now-visible distant box geometry lacked a grounded horizon. The next source revision lowers the solid shaft walls below ground, extends the Gallery floor, removes disconnected members, and substitutes a connected sculpted shoreline. These changes still require visual acceptance.
- Genuine browser tab zoom was exercised using `chrome.tabs.setZoom/getZoom` in an isolated Chrome-for-Testing profile with a task-local inspection extension. Twelve viewport/zoom combinations and their Atelier dialogs were captured in `.artifacts/overhaul/browser-zoom-candidate-1/`. The 1280x720/150% row FAILED visual layout: CSS viewport853x480 hid the header and put the action dock below the viewport. This is not a DPR emulation. The responsive CSS is being corrected and the full affected sweep will repeat. No user's browser profile or extensions were altered.
- Independent review decoded actual WebM frames for ordinary, knight, capture, castling, empty and loaded Shift. The motion gate FAILED: knight clipping through the pawn rank, capture/castling appearing instantaneous, and no strong loaded-Shift seating response. Passenger attachment during Shift travel passed. The next revision warms effect shaders before the animation clock, raises and phases the knight arc, stages capture contact/dissolve/seating, lengthens castle transit and adds a destination seating pulse. Footage review must repeat; final-position tests are insufficient.
- Frame measurements in `.artifacts/overhaul/frame-profile-world3/` exposed roughly700-1160 draw calls including repeated shadow passes. Fixed-light shadow caching reduces steady frames to roughly410-450 calls while moving casters invalidate the cache. Reduced-resolution bloom and Balanced FXAA replace expensive multisampled HDR rendering; High retains multisampling. The first FXAA profile recorded Gallery/Nocturne p95 near16.9ms, but Daylight orbit still reached33.4ms. This is useful progress, not a completed performance gate; static instancing and subsequent motion/bot/native measurements remain pending.

## Inspection reliability

`scripts/inspection/index.html` is a development-only actual-asset viewer served by Vite, with all six types/both sides, orthographic views, neutral/material/normal/depth/wireframe modes and shadow A/B controls. It is not part of the player build.

The first 64-sheet run was not accepted: the viewer requested an absent favicon, and visual inspection found cached floor shadows in a supposedly shadow-disabled sheet. The harness now explicitly disables light casting and mesh receiving/casting and invalidates the affected shader programs. The two-sheet neutral three-quarter shadow A/B recheck passed with zero console/page errors; both images were opened and the disabled case has no cast shadows. The original failed run is retained under `.artifacts/overhaul/piece-inspection/`. The corrected full run under `.artifacts/overhaul/piece-inspection-2/` captured all 64 sheets with zero console/page errors. That historical run was superseded by the later sculpture rebuild; no initial sheet is relabeled as corrected evidence.

## Latest verified checkpoint

- Independent review of `world-review-4` passes the art-direction entry gate: the three worlds are structurally distinct, the entrance is composed around the actual board, and the rift has physical depth. This is not final whole-product acceptance.
- The first 96-sheet asset review (`pieces-final-candidate`) FAILED both families for malformed bishop openings, weak knight views, unsupported details and contact artifacts. The open-surface test exemption was removed. Root then built bishops from capped clipped solids, refined horse anatomy and facial geometry, connected ornaments, grounded every asset at y=0 and corrected shadow bias. Two independent reviewers inspected every one of the new 96 sheets in `pieces-final-candidate-2` and PASS both asset families. Remaining Faceted end-on/top-view recognition limitations are non-blocking and require gameplay-scale checking. Subsequent metal/wood palette changes require a material-only recheck.
- The first full primary matrix captured all 216 actual views with zero console/page errors in `.artifacts/hierarchical-review/final-candidate-1`. Root reviewed all 72 Gallery and 72 Daylight rows through their contact sheets and opened suspicious full-resolution cases; the independent reviewer covered all 72 Nocturne rows and 31 originals. It FAILS as final evidence: the known older pieces remain in those images, and Black-view glare, Nocturne faceted-metal clipping and far dark-wood readability need correction. The current source changes roughness, highlight threshold and wood values; the matrix must repeat.
- `browser-zoom-candidate-2` repeats all twelve genuine tab-zoom combinations, confirms the requested zoom through the browser API, and asserts header/canvas/dock bounds. The original 150% defect is visually confirmed fixed. The final broader application, dialog and native scaling reviews remain pending.
- Static scenery instancing preserves source geometry and transforms while reducing scene objects; idle draw counts are now roughly320-360. Some recorded frame-time outliers remain. The renderer now prepares material programs before asset readiness completes; this needs final isolated timing and action/worker checks.
- All 39 tests in five files pass. Full-game and eighteen-row UI harnesses have been rebuilt without planned-resignation shortcuts, with source/build identity and resumable incomplete records. They have not yet run because the visual gate is still open.

## Required next work

The second full matrix, `final-candidate-2`, binds build `a9ba89cb510f625d32e0` to source `502076e`. Root inspected all 72 Gallery contact cells and four original detail/material views without a consequential blocker; independent review inspected all 72 Daylight cells and 52 originals and passed. Nocturne's 72-cell/31-original independent review failed only the broad lower-right flare in Black normal/far views. The corrected analytic water normals and lower nocturnal glint remove that flare in all 12 affected originals, independently reviewed with all hashes verified in `water-normal-fix`. That run's enclosing receipt remains failed because a later Daylight settings transition timed out; the Nocturne verdict is scoped and does not substitute for a completed final matrix.

Core motion candidate 3 passes ordinary, knight, capture, castling, en passant and empty Shift on decoded real video. Its loaded-rook clip skipped the final animated segment during synchronous PNG readback. The corrected WebM-only motion capture, `motion-seat-no-readback-2`, passes both loaded and empty Shift independently: the passenger remains stable and the seat pulse is visibly separate from final legal outlines. No choreography source change was necessary. Promotion video review and wood-material animation recheck remain pending. No final-position screenshot substitutes for intermediate motion proof.

The interaction audit found and source fixes address duplicate quiet draw offers, aggregate quiet-counter wording, keyboard destination focus, replay narration, temporary-H hint release, no-op Atelier animation cancellation, human-side bot draw/resignation attribution and narrow Learn links. All six combined real-UI checks pass in `ui-state-regressions/candidate-8` on build `d7b025366bfbd99cc160`, including an actual bot animation arriving while Atelier is open. Earlier failed harness runs remain preserved: they missed short animation windows or mishandled the intentional cancellation of intercepted worker requests. The persisted `showShifts` field remains solely for `rift-ui-save/1` round-trip compatibility; the renderer's obsolete visibility gate is removed, so a v1 preference cannot conceal legal Shift cues.

The next candidate narrows renderer invalidation: changing a piece family/material rebuilds the board, theme/quality changes rebuild the world, and a reduced-motion-only change does neither. This avoids reconstructing the architecture for unrelated settings. Independent source review additionally caught continuing camera/tile easing after reduced-motion enable and Three's asynchronous compiler polling disposed materials during rapid configuration. Both are repaired: transforms snap, and readiness now follows synchronous compilation plus an actual submitted frame, with canceled readiness rejected on disposal. All three labelled renderer-API stress probes pass in `renderer-state-regressions/candidate-1` on build `5345e003836e3f810c37`; selection, Explore/Return and the concluding ordinary move use real UI input. Final runtime/scaling cycles remain pending. `performance-playtest.mjs` replaces the narrower `frame-profile.mjs`; historical frame receipts remain intact. New timing distinguishes CPU render submission from unmeasured GPU/display latency.

The final isolated piece capture `pieces-palette-final` passes the material gate in both independent lanes. All 96 sheets were recaptured. SHA-256 correspondence proves 80 images are byte-identical to the accepted asset set; reviewers verified and opened all 16 changed metal/wood orthographic originals. No consequential glare, palette, detail, shadow or apparent-geometry defect remains in that asset gate. Source SHA-256: `6fd169ba7f65f79e55d8098c87c5cbc9ce81e68803eba94d8645c7fc473925d0`.

The first detail/state run exposed a real close-angle input defect: the B4 destination-hole ray hit A4's lower side/undercarriage behind the opening, preventing the loaded-Shift preview. Picking now uses the board-plane hole as an input boundary while preserving nearer visible piece/handle hits. The exact distance-7 angled reproduction passes preview and knight-promotion commitment in `renderer-state-regressions/candidate-2`, build `ed2dcefa0532feabc588`; all four renderer probes pass. The failed detail capture remains intact and the unchanged detail harness is repeating on the repair. No camera workaround replaces this fix.

## Current visual and interaction closure

The fourteen original application/mobile Undo captures reviewed across `application-final-candidate-2` and `ui-state-regressions/candidate-9` pass: the phone Undo control is reachable, approval/cancel is an in-app dialog, and every authored camera plus distance-16 sample fits the playable board. The prior phone crop and hidden-Undo findings are closed.

All four lessons now preserve the original match while using a separate practice table. A visible Return to match control restores the original record and policy; reload restores the original autosave, while manual export deliberately exports the current lesson. The actual-UI `tutorial-preservation/candidate-1` passes all four lessons, switching among lessons before returning, reload, explicit New/Import and export semantics on build `64c7c8a9795d0795a491`. `ui-state-regressions/candidate-10` passes seven checks on that same build, including repeated quiet prompts and mobile Undo. The play guide now describes these actual controls and flows.

Independent real-video review passes all eight ordinary/Shift promotion classes in `promotion-no-readback`, plus wood capture/ordinary promotion in `wood-motion-final` and wood Shift promotion in `wood-shift-motion-final`. Earlier interrupted or missing-frame runs remain failed or limited evidence. Isolated VP8 artifact frames and observed capture gaps are documented limitations; no final screenshot substitutes for motion review.

The six-experience review of `world-final-candidate` FAILED the entrance: an approximately eight-second dark startup and a short blank return frame undermined the scene. Architectural worlds, rift depth, loaded Shift and craft passed with recorded polish reservations. The matched twelve actual v1/new captures in `matched-before-after` preserve verified immutable v1 bytes and matched semantic settings, with authored camera differences stated.

A startup CPU profile in `.artifacts/startup-profile/baseline.json` attributes most cold-start time to shader first-use checks, including an interim draw before final textures/reflections. The repair defers that interim draw, paints loading feedback, starts an authored entrance after the finished frame, and performs buffer resizing immediately before drawing. Independent video review of `entrance-repair-1`, build `c6574eef5dfc5a5d2106`, passes the continuous 2.6-second live reveal and confirms that Return no longer blanks. Cold readiness remains about 12.07 seconds and is an open performance issue. The same-build `renderer-state-regressions/candidate-4` passes all four state probes; candidate-3's earlier 30-second bootstrap timeout remains recorded. The shared UI driver now waits for actual readiness before clicking launch controls and gives initial bootstrap a bounded 60-second timeout; this is test synchronization, not a speed claim.

Independent review opened all 24 original images in `browser-zoom-final-candidate` (three viewport sizes, four actual zoom factors, app plus Atelier) and passes core controls, board fit and dialogs. Native development validation passes all eight original images in `native-visual-candidate-5`: two actual window sizes, four Electron zoom factors, and separately recorded OS display scaling of 1.5. Both runs bind identical start/end manifests to `c6574eef5dfc5a5d2106`. Narrow layouts make secondary drawers scroll-dependent while keeping core controls visible. This is not packaged acceptance.

Native preflight also exposed an Electron 44 entry regression: `require.main` identifies Electron's bootstrap under its ESM loader, so the old guard never opened a window. The entry now starts only in Electron's browser process; Node boundary imports stay inert. Initial native captures are retained as failed evidence: candidate-1 had no window, candidate-2's diagnostic fetch correctly failed the renderer's `connect-src 'none'`, candidate-3's Playwright screenshots cropped OS-scaled content, and candidate-4's direct capture returned a previous zoom's compositor frame. The final harness inspects static protocol bytes through main-process `net.fetch` and captures the second fresh native compositor frame after layout, without relaxing the product CSP.

The explicit checking-move video passes static threat clarity but initially lacked authored check-event motion. The new `check-event-pulse` capture on build `07721c97fec519f7f175` adds an 800-ms expanding coral king ring and restrained rift response, then retains the existing threat marker. Independent video review passes its visible expansion/fade and tactical clarity; combined with capture/promotion evidence, flagship 5 passes normal-motion review. Independent source review finds no pulse leak, stale event or duplicate frame loop. `renderer-state-regressions/candidate-5` passes five checks, including disabling the effect during an observed pulse and a subsequent actual checking move with reduced motion; the static threat cue remains visible.

`startup-candidate-1` passes seven actual-UI/runtime checks on `07721c97fec519f7f175`: all four launch choices with the texture deliberately held before readiness, an untouched post-frame camera reveal, fresh OS reduced motion, and an actual saved bot-to-move reload. The reload starts zero workers while assets are held and exactly one fresh worker afterward, producing one real reply. `startup-candidate-2` repeats all seven checks with explicit final-camera endpoint assertions and a corrected expected-abort observation window. Early bootstrap timestamps are recorded separately from full cold readiness. All 39 TypeScript tests and all 67 original-reference Python tests pass after the source changes.

1. Resolve the remaining measured performance tails. The final full-board matrix below passes; all six flagship experiences have scoped independent visual evidence, with final combined motion/application review in progress.
2. Preserve completed geometry/material, motion, detail and scaling evidence with exact source correspondence; repeat dependent visual rows changed by subsequent fixes.
3. Only after the visual/experiential gate, run the complete 18-session mode/layout/policy matrix, six complete real-UI games, tutorials, special rules/endings and recovery/interruption scenarios. Update legacy harness assumptions to the new visible UI instead of bypassing it.
4. Keep instrumented cause profiles separate from the isolated frame-pacing measurements below, with scene, shadows and postprocessing counted.
5. Verify existing-save/service-worker upgrades, final offline Windows packaging and public-origin behavior; publish a new tagged artifact only after all acceptance gates pass. Preserve v1 as historical evidence.

## Current performance repair

`performance-candidate-1` completes all 22 isolated samples on `07721c97fec519f7f175`, with no runtime errors, but does not pass the frame targets. Gallery Balanced records roughly 19.3-ms idle and 21.9-ms orbit CPU medians; Daylight orbit has a 1.12-second maximum. The first loaded-promotion sample records a 1.41-second CPU maximum. Raw 60/30-fps comparisons are retained, including nominal refresh-quantization misses, without rounding them into passes.

The four-stage CPU probes in `performance-probe-1/2/3` identify repeated shader-program disposal/recompilation in highlights and fades, plus GPU synchronization from the metrics helper. The repair caches immutable highlight resources and pooled fade materials, warms the current transparent-piece programs during normal frame readiness, starts each animation clock after its first complete draw, and caches renderer identity instead of querying the GPU on every metrics poll. Program-query stalls disappear from the dominant profile samples for both first and repeated loaded promotion in probe 3. These profiles are diagnostic and instrumented, not target-framerate measurements; part of the improvement removes observer overhead.

Tile shell geometry/materials are preserved but submitted through five instanced batches instead of 84 shell meshes. The 56 square surfaces and all piece meshes retain their exact pick ownership. Shell matrices follow each moving/lifted tile, and instance hits retain tile identity behind the existing hole-depth barrier. Coordinate labels share one glyph atlas and merged mesh, preserving their original positions, font and facing. Orbit gestures no longer animate incidental hover lifts and invalidate shadows continuously. Independent source review passes ownership, cancellation and disposal. The renderer-state recheck `candidate-7` passes all five probes on `1da2c22231750ccee21e`, including the exact near-hole loaded Shift. Its predecessor `candidate-6` exposed stale camera matrices during rapid preset/input changes; explicit projection/pick matrix updates repair that defect. Final imagery and isolated frame pacing are repeating on this source.

`performance-candidate-2` completes all 22 samples on clean source `fd892d5`, build `1da2c22231750ccee21e`. Gallery Balanced idle/orbit CPU medians are now both about 6.6 ms, with 219/212 draws and zero orbit shadow refreshes; bot and capture p95 callback intervals are about 17 ms. One 383-ms Daylight orbit stall remains. A targeted CPU profile attributes it to a material first encountered outside the initial camera view. The next repair submits off-camera materials during one normal readiness draw, restoring all original culling flags synchronously afterward. Its targeted profile removes the program-query stall; final uninstrumented measurements remain pending.

The same-browser, same-viewport `refresh-cadence-control` records 300 empty-page callback intervals: mean 59.995 Hz, median 16.7 ms, p95 16.9 ms, p99/max 17 ms. Even this unloaded control fails the strict `1000/60` comparison on 167 intervals. It explains those small cadence/quantization differences; it does not excuse large stalls or replace game measurements. Original raw deadline flags remain unchanged.

`performance-candidate-3` completes all 22 samples on clean `b78e6f0`, build `8960c217b7be87577b35`, without errors or recorded Long Tasks. Every p95 is at or below 16.9 ms; all eleven Low samples satisfy their raw 30-fps floor. Independent review accepts bounded near-60-Hz Balanced throughput but identifies isolated 33.5-ms idle, 33.3-ms bot and 49.8-ms loaded-Shift intervals. A dedicated nine-sample raw-interval follow-up is prepared; no every-frame 60-fps claim is made.

The final primary matrix `final-candidate-3` captures all 216 views on that same clean source/build with identical final assets. Three independent reviewers inspect all 72 cells per environment and 59 full-resolution originals in total. All three environments PASS with recorded minor far-detail/contrast limits; all 216 indexed original hashes verify. Gallery's far foreground arch stays below playable squares and pieces, and Nocturne's prior flare is absent. Root also inspects every Gallery cell and seven originals. Scoped verdicts are in the artifact's `independent-visual-verdicts.json` and `gallery-root-review.json`. All sixteen final motion cases have been captured on the same build and await video inspection. Full games remain gated on completing the remaining visual rows.

The isolated follow-up `performance-tail-final-1` completes exactly nine fixed samples, 300 raw intervals each, on clean `ce939cb` with unchanged build `8960c217b7be87577b35`. It confirms an unresolved stable-60 gate: loaded Shift maxima are 50.1/33.5/83.3 ms; bot maxima are 66.6/33.2/50.1 ms. One repetition of each reaches 33.3-ms p95. A 57-ms Long Task follows the third promotion click; a separate 70-ms task occurs at the end of the third idle probe. Independent adjudication calls for a bounded cause trace covering promotion setup, bot-period contention, and GC/host scheduling. All strict raw flags remain false; the refresh control does not excuse these larger gaps.

Current-build native development scaling in `native-visual-release-candidate` captures eight fresh compositor originals and independently passes all eight at OS scale 1.5, two window bounds and four Electron zoom factors. The smallest viewport stacks secondary drawers below the fold while retaining the board and core action dock. This remains development-only evidence and contains no open-dialog images; final packaged acceptance remains required. Root opens the worst-size/highest-zoom original as an additional check. Current-build browser scaling and sixteen motion clips are under independent review.

The first current-build detail/resource run, `detail-state-release-candidate`, retains eight successful detail/quality captures but FAILS a 30-second Apply click during the subsequent Nocturne configuration. It records no browser error and its failure image shows the applied configuration. This is not a completed detail/resource gate. Repeat under isolated capture conditions after video decoding, preserving the failed receipt and checking whether the delay reproduces.

The isolated repeat `detail-state-release-candidate-2` completes all thirteen captures and all 24 configuration cycles without errors. An independent reviewer opens all thirteen originals and passes near passenger/cavity/seam/corner detail, quality/contrast samples, check text/ring, modal/focus return and responsive access. Its twelve measured samples remain within the like-for-like warm resource bounds. The earlier timeout does not recur and remains recorded. All eight core videos in `motion-final-candidate-4` also pass independent sampled review; promotion refinement remains in progress. `world-release-candidate` completes fourteen current-build captures and a real entrance/exploration recording with no errors and preserved save-bypass behavior.

The bounded cause trace `tail-cause-profile-1` attributes about 9 ms of its promotion-close sample to effect geometry construction and about 10 ms to redundant full-scene compilation; board reconstruction and smaller GC work contribute too. Large GPU trace gaps remain unattributed diagnostic observations, not frame-frequency evidence. Independent source review supports removing only per-action compilation while keeping startup/configuration preparation, and reusing exactly the same immutable Shift/capture/promotion geometry. The existing first-submitted-frame animation clock and per-instance transforms/materials remain. Check-pulse geometry retains its separate disposal ownership. All 39 tests and the production build pass after this change; build `63f6dfc0a3f4b0f16b2f` still requires runtime/performance and dependent motion rechecks.

`renderer-state-regressions/candidate-8` passes all five probes on build `63f6dfc0a3f4b0f16b2f`. All eight baseline promotions now pass independent review of the actual `8960` videos, including targeted transform frames; together with the core review this closes all sixteen baseline cases. The 24 `browser-zoom-release-candidate` originals also pass independent review, including every Atelier dialog. These retain their exact prior-build scope; no later-build motion claim is inferred from them.

The optimized build does **not** close performance acceptance. `performance-tail-final-2` measures nine fixed repeats without errors but retains substantial tails. `performance-candidate-4` completes all 22 activities without errors; one Low Nocturne orbit has a 650-ms interval and Balanced Daylight orbit has 633.4 ms, both without a recorded Long Task and with render-submission maxima below 21 ms. Their cause remains unclassified; neither the instrumented trace nor those CPU timings proves a GPU/OS cause. Balanced bot p95/max is 33.4/66.7 ms with a 54-ms Long Task. Low loaded Shift improves to a 16.8-ms maximum in this bounded run, but four of eleven Low activities fail the raw floor comparison. Preserve all raw flags. Both performance targets remain unresolved release gates while the independent final visual and complete-product checks continue.
